/**
 * WorkspaceShellImpl — stub export kept for import compatibility.
 * The real workspace tab header lives in MainLayout as WorkspaceShellHeader.
 */
export function WorkspaceShell() {
  return null;
}
